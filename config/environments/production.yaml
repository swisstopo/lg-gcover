# Production environment overrides

global:
  log_level: WARNING
  proxy: ${GCOVER_PROXY_URL}
  s3:
    bucket: ${GCOVER_S3_BUCKET_PROD}
    profile: ${GCOVER_AWS_PROFILE}
    upload_method: "auto"
    upload_timeout: 30
  logging:
    file:
      path: "\\\\adb.intra.admin.ch/swisstopo$/Transfer/mom/gcover_prod_{date}.log" # System log location
      retention: "90 days" # Keep production logs longer
    console:
      format: "simple" # Clean console output
      show_path: false # No file paths in production
    modules:
      modules:
        "gcover": "INFO" # Standard logging
        "urllib3": "ERROR" # Only HTTP errors
        "boto3": "ERROR" # Only AWS errors
qa:
  db_path: "data/prod_verification_stats.duckdb"

gdb:
  base_paths:
    backup: "\\\\v0t0020a.adr.admin.ch/iprod/backup/GCOVER"
    verification: "\\\\v0t0020a/topgisprod/10_Production_GC/Administration/QA/Verifications"
    increment: "\\\\v0t0020a.adr.admin.ch/iprod/backup/Increment/GCOVERP"
  processing:
    max_workers: 2
    compression_level: 9
  db_path: "data/prod_gdb_metadata.duckdb"


